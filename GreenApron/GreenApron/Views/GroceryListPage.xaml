<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GreenApron.GroceryListPage"
             Title="Grocery List">
  <ContentPage.Content>
    <StackLayout VerticalOptions="FillAndExpand">
      <Button Clicked="MarkAsBought" Text="Mark As Bought" TextColor="White" BackgroundColor="#77D065" />
      <ListView x:Name="groceryList" VerticalOptions="FillAndExpand">
        <ListView.ItemTemplate>
          <DataTemplate>
            <TextCell Text="{Binding Ingredient.ingredientName}" Detail="{Binding AmountUnit}" />
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>