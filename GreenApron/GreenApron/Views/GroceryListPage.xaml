<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GreenApron.GroceryListPage"
             Title="Grocery List">
  <ContentPage.Content>
    <StackLayout VerticalOptions="Start">
      <Button Clicked="MarkAsBought" Text="Mark As Bought" TextColor="White" BackgroundColor="#77D065" />
      <Button Clicked="OpenActionView" Text="Open Action View" TextColor="White" BackgroundColor="#77D065" />
      <Button Clicked="OpenProductSearch" Text="Add a Grocery Item" TextColor="White" BackgroundColor="#77D065" />
      <Label Text="Tap any item to edit it's quantity or to mark as purchased." />
      <ActivityIndicator
                           IsVisible="{Binding IsBusy}"
                           IsRunning="{Binding IsBusy}"
                           Color="Black" />
      <ListView x:Name="groceryList" VerticalOptions="FillAndExpand"
        GroupDisplayBinding="{Binding Title}"
        GroupShortNameBinding="{Binding ShortName}"
        IsGroupingEnabled="true"
        ItemTapped="OnItemTapped">
        <ListView.ItemTemplate>
          <DataTemplate>
            <TextCell Text="{Binding Ingredient.ingredientName}" Detail="{Binding AmountUnit}" />
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>